<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>RECN Nevada ROI Calculator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body { font-family: Arial, sans-serif; margin:0; padding:20px; background:#f8f9fa; color:#333; }
    h1 { text-align:center; color:#007bff; }
    form { max-width:600px; margin:20px auto; background:#fff; padding:20px; border-radius:10px; box-shadow:0 2px 6px rgba(0,0,0,.1); }
    label { display:block; margin:12px 0 4px; font-weight:bold; }
    input { width:100%; padding:10px; border:1px solid #ccc; border-radius:6px; }
    button { margin-top:20px; padding:12px 20px; background:#28a745; border:none; color:#fff; font-weight:bold; border-radius:6px; cursor:pointer; width:100%; }
    button:hover { background:#218838; }
    #result { display:none; margin-top:20px; padding:20px; background:#d4edda; border-radius:8px; text-align:center; font-size:18px; }
    footer { text-align:center; margin-top:40px; font-size:13px; color:#666; }
  </style>
</head>
<body>
  <h1>📊 Nevada Housing Benefit ROI Calculator</h1>

  <form id="roiForm">
    <label>Number of Employees</label>
    <input type="number" id="employees" placeholder="500">

    <label>Annual Turnover Rate (%)</label>
    <input type="number" id="turnover" placeholder="15">

    <label>Average Replacement Cost ($)</label>
    <input type="number" id="replacement" placeholder="25000">

    <label>Annual Housing Benefit Budget ($)</label>
    <input type="number" id="budget" placeholder="150000">

    <button type="button" onclick="calculateROI()">Calculate ROI</button>
  </form>

  <div id="result"></div>

  <footer>
    © 2025 Real Estate Concierge Network | Nevada Division
  </footer>

<script>
function calculateROI() {
  var employees = parseFloat(document.getElementById('employees').value || 500);
  var turnover = parseFloat(document.getElementById('turnover').value || 15)/100;
  var replacement = parseFloat(document.getElementById('replacement').value || 25000);
  var budget = parseFloat(document.getElementById('budget').value || 150000);

  var separations = employees * turnover;
  var reduction = 0.30; // assumed retention lift
  var savedReplacements = separations * reduction;
  var savings = savedReplacements * replacement;

  var net = savings - budget;
  var roiPct = budget > 0 ? (net / budget) * 100 : 0;
  var paybackMonths = savings > 0 ? (budget / savings) * 12 : Infinity;

  var result = `
    <strong>Estimated Annual ROI:</strong> ${Math.round(roiPct)}%<br>
    <strong>Annual Savings:</strong> $${Math.round(savings).toLocaleString()}<br>
    <strong>Payback Period:</strong> ${isFinite(paybackMonths) ? paybackMonths.toFixed(1)+' months' : '—'}
  `;

  document.getElementById('result').innerHTML = result;
  document.getElementById('result').style.display = 'block';
  document.getElementById('result').scrollIntoView({behavior:"smooth"});
}
</script>
</body>
</html>
